{
  "name": "clickhouse-bot",
  "version": "0.0.0",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@3.3.0",
  "type": "module",
  "main": "src/index.mjs",
  "bin": "./bin.cjs",
  "files": [
    "src",
    "bin.cjs",
    "LICENSE.md",
    "README.md"
  ],
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "prettier -c .",
    "build": "esbuild src/bin.mjs --bundle --platform=node --target=node18 --outfile=bin.cjs",
    "start": "node src/bin.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.15.15",
    "prettier": "^2.7.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "puppeteer": "^19.2.2"
  }
}
